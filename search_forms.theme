<?php

/**
 * Implements hook_theme().
 */
function search_forms_theme() {
    return [
      'search_forms' => [
        'variables' => [
          'forms' => [],
        ],
        'template' => 'search-forms', // Matches the Twig template name.
        'path' => drupal_get_path('module', 'search_forms') . '/templates', // Path to your template.
      ],
    ];
  }
  